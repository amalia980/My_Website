const text = document.getElementById("greeting");

const myArray = ["Hello", "Bonjour", "Hola"];

function greeting() {
    for (i = 0; i < myArray.length; i++) {
        //console.log(myArray[i]);
        //text.innerText = myArray[i];
    }
}
greeting();